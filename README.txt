To generate the list that includes stop words call the function SeparateStoryWords(fileName). Where fileName will be the name of the text file you want to use so ex. SeparateStoryWords(hamlet.txt).

To generate the list that does not include stop words call the function SeparateStoryWordsWithoutStopWords(fileName).

To generate the graph use the function MakeBarGraphs().
